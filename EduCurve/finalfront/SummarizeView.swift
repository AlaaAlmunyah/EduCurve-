import SwiftUI

struct SummarizeView: View {
    var body: some View {
        Text("Summarize View")
            .navigationBarTitle("Summarize", displayMode: .inline)
    }
}

struct SummarizeView_Previews: PreviewProvider {
    static var previews: some View {
        SummarizeView()
    }
}

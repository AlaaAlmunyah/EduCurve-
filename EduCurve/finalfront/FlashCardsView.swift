import SwiftUI

struct FlashCardsView: View {
    var body: some View {
        Text("Flash Cards View")
            .navigationBarTitle("Flash Cards", displayMode: .inline)
    }
}

struct FlashCardsView_Previews: PreviewProvider {
    static var previews: some View {
        FlashCardsView()
    }
}
